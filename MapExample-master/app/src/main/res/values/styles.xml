<resources>

    <!-- Base application theme. NoActionBar because we are using a Toolbar -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Set AppCompat’s actionBarStyle -->
        <item name="actionBarStyle">@style/AppTheme</item>

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColor">@color/primary_text</item>

        <!--<item name="android:windowBackground">@drawable/mapbkg</item>-->  <!--Background image-->

    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@color/accent</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/primary_text</item>
        <!--dialog background-->
        <item name="android:textColorSecondary">@color/primary_text</item>
        <!--dialog background-->
        <item name="android:background">@color/dialogBackground</item>

    </style>

    <style name="MyPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <!--buttons color-->
        <item name="colorAccent">@color/accent</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/primary</item>
        <!--dialog background-->
        <item name="android:textColorSecondary">@color/primary</item>
        <!--dialog background-->
        <item name="android:background">@color/primary_light</item>


    </style>

</resources>
